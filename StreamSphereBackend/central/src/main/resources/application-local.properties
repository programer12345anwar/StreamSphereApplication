# Local profile settings — enable programmatic DB seeding for development
app.data.seed=true

# Allow frontend dev server origins (adjust if your dev machine IP differs)
app.cors.allowed-origins=http://localhost:5173,http://localhost:8080